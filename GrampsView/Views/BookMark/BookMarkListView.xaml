<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GrampsView.Views.BookMarkListPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ucon="clr-namespace:GrampsView.UserControls"
    Title="{Binding BaseTitle}"
    ControlTemplate="{StaticResource ListPageTemplate}"
    Style="{DynamicResource GVBaseContentPage}"
    mc:Ignorable="d">

    <!--  Content  -->
    <Grid>

        <ucon:FlexSingleCardType
            x:Name="flexLayout"
            BorderColor="{DynamicResource SecondaryVariantLightColor}"
            FsctSource="{Binding BookMarkSource, Mode=OneWay}"
            FsctTemplate="{StaticResource BookMarkCardSmallDT}" />
    </Grid>
</ContentPage>